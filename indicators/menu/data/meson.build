install_data(
    'com.github.hcsubser.hybridbar.menu.gschema.xml',
    install_dir: join_paths(datadir, 'glib-2.0', 'schemas')
)

install_data(
    'hybridbar-applications.menu',
    install_dir: join_paths(get_option('sysconfdir'), 'xdg', 'menus')
)
